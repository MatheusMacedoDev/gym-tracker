import styled from 'styled-components/native';

const ContainerInputsProfile = styled.View`
  flex-wrap: wrap;
  flex-direction: row;
  justify-content: space-between;
  padding: 10px;
`;

export default ContainerInputsProfile;
