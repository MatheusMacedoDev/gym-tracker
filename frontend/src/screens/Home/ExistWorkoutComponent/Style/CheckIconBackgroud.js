import styled from 'styled-components';

export default CheckIconBackgroud = styled.View`
    width: 28px;
    height: 28px;
    background-color: ${props => props.color};
    border-radius: 30px;
    justify-content: center;
    align-items: center;
`;
