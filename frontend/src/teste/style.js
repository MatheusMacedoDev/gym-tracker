import styled from "styled-components/native";

import { StyleSheet } from 'react-native';

export const BtnExcercise = StyleSheet.create({
  button: {
    height: 60,
    backgroundColor: '#2F2C32',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 5,
  },
  buttonTextEx: {
    color: 'white',
    fontSize: 16,
  },
});
