import styled from "styled-components/native";
import { colors } from "../../../colors.config";

export const TitleExerciseCard = styled.Text`
    font-family: "Montserrat_600SemiBold";
    font-size: 18px;
    color: ${colors.gray};
`;