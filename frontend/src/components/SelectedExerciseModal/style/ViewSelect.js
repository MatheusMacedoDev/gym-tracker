import styled from 'styled-components';

export default ViewSelect = styled.View`
    margin-top: ${props => props.marginTop};
    width: 85%;
    flex-direction: row;
    justify-content: space-between;
    align-self: center;
`;
